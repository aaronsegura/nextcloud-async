ARG NEXTCLOUD_VERSION

FROM nextcloud:$NEXTCLOUD_VERSION-apache

ARG GROUPFOLDERS_URL
ADD ${GROUPFOLDERS_URL} /tmp/

ARG GROUPFOLDERS_ARCHIVE_NAME
RUN true \
	&& tar xf /tmp/$GROUPFOLDERS_ARCHIVE_NAME -C /tmp/ \
	&& true
